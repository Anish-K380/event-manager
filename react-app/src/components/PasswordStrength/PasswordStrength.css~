.password-strength-container {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-top: -4px; /* pulls it closer to input */
}

.password-strength-text {
  font-size: 0.75rem;
  font-weight: 500;
  opacity: 0.85;
}
