import asyncpg

async def connect():
    conn = asyncpg.connect(
        user = 'user',
        host = 'localhost',
        database = 'mydb',
        port = 5433,
        password = 'password')

    return conn
